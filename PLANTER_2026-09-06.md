# 原生育苗自動化（v152，基本流程已實機驗證）

## 邊界

使用者要求自動拔成熟花苗、空槽補種。實機現有兩株已種花苗：一株達 1000 步、一株仍成長。不得修改步數、提前完成、使用額外孵化器、購買容量、放生皮克敏或丟棄花苗。既有批次／花田／200m／搬運領取策略不變。

新控制檔 `/data/local/tmp/pikmin-planter-mode.txt`，必須 0644：

- `off`：僅讀取育苗庫存，預設值。
- `pluck`：只拔永久槽位、已成熟且未標記 DisableConsecutivePluck 的花苗。
- `auto`：拔苗並填補空永久槽；所需步數最少優先，同步數按 ID 穩定排序，略過加星與 `DisableConsecutivePluck` 特殊花苗。這不是依取得時間排序。

## 實作與依據

`zygisk-module/cpp/planter_automation.inc` 重用既有遊戲回呼／InventoryManager／RpcManager，不新增 hook。新 managed 呼叫使用 `il2cpp_runtime_invoke`，完整 MethodInfo、例外捕捉與 64-bit GC handle；不直接呼叫未知泛型 ABI。

以實機 v152 dump/libil2cpp（254505288 bytes）核對：

- `Niantic.Ichigo.Utils.PikminSeedProtoExtensions.IsReadyToPluck`，RVA `0x6CDE6B8`：PlantedTimeMs 非零且 CurrentSteps >= RequiredSteps。額外要求 required > 0，與實際槽位同 ID。
- `PlanterSlot.Start`，RVA `0x5897F2C`：SlotType 0 選 permanentSlotPrefab，1/4 選額外槽位 prefab。只操作已存在、index 在 NumMaxSlots 範圍內且 type=0 的槽，不擴充槽位。
- `InventoryManager.GetPikminSeedList` `0x7298528`；透過 public getter 讀 Planter、種子、Pikmin、ItemCapacity。Pikmin 容量取 CapacityProto.CurrentCount（與 InventoryCapacityManager 分支相同），未知／滿倉暫停。
- `SendPullPikminRpcAsync` `0x7259FE4`：每次一個 seed ID。RepeatedField.Add 以實際 String 參數型別選定 MethodInfo。
- `SendSetPikminSeedRpcAsync` `0x725BB2C`：seed ID＋明確 SlotIndex；Point 沿用種子的 BirthPlacePoint，與 PelletPlanter 原生流程 `0x584E60C` 相同，**不是擅自指定目前 GPS**。
- RpcRetryPolicy=0（ThrowException），無盲目重送。RPC 成功必須 Status.Status=1；拔苗還須 response 的新 Pikmin ID 原先不存在、現已入庫，同 seed 已消失、槽位不再指向該 seed。種植須同 seed 的 planted time > 0、同槽位指向該 seed。

每 5 秒觀測，一次一筆；前筆確認後至少 5 秒才允許下一筆。這與花田「種花」是不同功能，不操作 GPS、花瓣或種花開關。

## 持久狀態

遊戲私有 `files/`：

- `planter_inventory.tsv`：v1/time/PID/Pikmin count/capacity；slot/seed rows；end seed-count。只有完整庫存才原子取代。
- `planter_status.tsv`：v1/time/PID/event/mode/detail。
- `planter_history.tsv`：最近 24h、最多 500 筆；送出、伺服器成功、精確庫存確認分開紀錄。
- `planter_pending.tsv`：送出前先 fsync 寫入 seed/action/slot。結果不明不重送；重啟後若仍存在則 fail closed，需人工核對，不能盲目刪檔解鎖。

操作 off 不會取消已送出 RPC，仍只觀察該筆結果。未知／失敗狀態保留 pending 證據，不自動重試。復原先核對伺服器及完整庫存，停用育苗後由開發者處理 pending；首版無一鍵強制重試。

## 驗證狀態

- 24 項 C++ 政策測試在 Android14 `.202:5555` 通過。
- 既有 5 處 Enumerator MethodInfo 靜態回歸通過。
- CC 0.6.10/code27 已編譯／安裝，11 項育苗 parser、7 項領取標籤測試通過。
- 1.4.16/code50 首輪只讀被 metadata guard 擋住，`IsReadyToPluck` namespace 誤用 Utils.Proto；未送出操作，遊戲無此功能造成的崩潰。修正為 Utils，1.4.17/code51 繼續測試。
- **1.4.17/code51 + CC0.6.10/code27 已完成基本實機驗收，dist 更新為1.4.17。** 詳見下方精確證據；不等於所有例外與長時間循環均已驗收。

### 2026-09-06 Android14 驗收

- PID10997：唯讀取得兩個永久槽，藍色1000/1000 ready、岩石0/5000 growing；49筆花苗，880/1200皮克敏。
- 09:13:44.153（1788657224153）只送藍色成熟株 Pull；09:13:49.214 `pluck-confirmed`。Status=SUCCESS，回傳新Pikmin ID先前不存在、現已在完整Pikmin庫存，同seed消失、槽位1清空，總數881。
- 09:14:24.537（1788657264537）只送一筆 Set；按最短步數選到未加星100步金色花苗，明確SlotIndex=1。09:14:29.605 `plant-confirmed`：同seed.PlantedTimeMs>0，同槽指向該seed，0/100 growing。
- 原岩石株ID、0/5000步未變。排除上述拔出／補種目標後，其餘47筆seed資料逐列完全相同。
- 各一筆sent、server-success、confirmed，pending檔已正常移除；未重複請求。遊戲PID10997持續，無遊戲crash。
- native dispatch_safety、managed_gc、return_policy（24項）、planter_policy（24項）全數裝置測試通過；既有泛型呼叫靜態測試通過。
- 模式收尾：dispatch=armed、return=all已還原，育苗auto保留啟用，三者0644；GPS／隊列／明信片策略未改。

建置SHA256：ZIP `962e3e33cea73f51b997c4c2a204e84d08ca93cb72e544b362c01500f56f6b1d`；安裝SO `baa9ec3abb0d14abeefb24b38d871ccb2fb71ae7154e6cb21b030e98a2123f12`。

尚待：真實步數成長後反覆拔補循環、自然滿倉、全加星／無候選、額外槽位已種成熟株、斷線與重啟未確認RPC恢復。上述安全分支有程式保護／部分政策測試，沒有宣稱均經實機觸發。一次性槽位明確不自動操作。RPC在遊戲執行／登入完成時才會運作，非關閉遊戲後仍獨立執行的服務。

本輪備份在唯一測試手機 `.202` 的 `/data/local/tmp/pikmin-planter-20260906`（root-only）：原 module/SO、CC0.6.9 APK、1.4.15 rollback ZIP、原控制檔。先前 carry 證據目錄不變。不要使用曾崩潰的 1.4.10。
