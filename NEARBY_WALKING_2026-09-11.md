# 移動中一般派遣：1.4.28/code62（本機開發）

使用者以JoyStick約18km/h循環路線散步，舊8m固定基準圈在約1.6秒就重置，而就緒至少需3秒，造成正常移動也難以派遣。本次只修改一般armed，沒有操作手機、JoyStick、安裝或發布。

## 修改

- `NearbyLocationGate` 比較相鄰不同時間的定位，改用7m/s乘時間差＋2m誤差容許，涵蓋5m/s正常移動；不要求固定方向，允許路線轉彎／折返。正常位移不改epoch，不取消已預約隊伍。
- 保留原始／遊戲處理後定位差≤8m、定位≤5秒新鮮、至少3份不同定位＋3秒暖機、心跳及時鐘保護。不放寬遊戲追趕延遲；若18km/h時遊戲持續落後超過8m仍可能等待，須實機觀測，不能保證本次已解決所有不派遣。
- 超速／跳點、同timestamp卻改變原始座標、舊定位、斷流、追趕會重置epoch及待送隊伍。已到新位置後仍重新暖機。有限頻率定位無法完美區分所有小跳點與正常移動。
- 一般armed候選掃描5秒→1秒；選隊後跨tick重驗至少1秒。批次／farm／off掃描仍5秒，批次選隊等待1500ms不改；返程診斷仍5秒。
- 五分鐘原生往返、可用隊員、CanTryStart、搬運力、送出前同隊核對、3筆in-flight上限及隊員預約不改。正常循環不暂停／接管JoyStick。送出後繼續移動仍可能延長返程。
- TSV沿用v2和既有狀態字串；`location-moving`現在代表位移不連續／超限，不代表所有行走。

## 驗證與交付

MethodInfo靜態檢查和NDK r27d arm64模組建置通過。新增18km/h兩分鐘連續軌跡、折返、全跳點、持續超速、重複timestamp與落後20m回歸案例，保留原有300秒邊界／台灣日本分段跳點／過期／休眠案例。policy測試只交叉編譯，不算執行通過；本機無可用host C++ compiler，Android測試依使用者要求延後。不得使用合成座標寫入遊戲來宣稱實測。

模組包含前次1.4.27唯讀餵食探查，沒有餵食／收瓣執行器；Control Center現有0.11.0/code55 APK本次未變更、不重建。先前1.4.27 ZIP保留在 `C:/Users/Ody/.codex/tmp/feeding-rollback-20260910/native-1.4.27.zip`；真正舊1.4.26回退ZIP同目錄 `native-before-feeding.zip`。

下一次：確認手機／版本並備份→執行native policy測試→依使用者允許安装新native並重開→確認1.4.28載入及同PID回報→先測靜止派遣，再測18km/h有效水果／花苗、循環轉角、三筆隊員互斥→遠距跳點期間0提早派遣→確認返程領取。核對 `nearby_gate_status.tsv`、`dispatch_history.tsv`、`dispatch_selection_diagnostics.tsv`，同時留意1秒掃描對大量任務與遊戲幀率的影響。

自動餵食先做唯讀資料核對，接妥執行器後才能測實際消耗。新機部署包與白名單不自動升級為此未驗收版本。
